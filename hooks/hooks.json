{
  "hooks": {
    "SessionStart": [
      {
        "commands": [
          {
            "command": "npx diverger-claude check --quiet 2>/dev/null || true",
            "timeout": 10000
          }
        ]
      }
    ]
  }
}
